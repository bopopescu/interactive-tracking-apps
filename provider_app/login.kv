<LoginScreen>:
    BoxLayout:
        orientation: 'vertical'
        padding: '20sp'
        height: "5dp"
        Label:
            text: 'Enter Username'
            pos_hint: {'center_x': .5, 'center_y': .5}
            color: (0,0,.75,1)
        TextInput:
            id: username
            pos_hint: {'center_x': .5, 'center_y': .5}
            size_hint: (.5, None)
            height: '30sp'
            hint_text : 'Username'
        Label:
            text: 'Enter Password'
            pos_hint: {'center_x': .5, 'center_y': .5}
            color: (0,0,.75,1)
        TextInput:
            id: password
            pos_hint: {'center_x': .5, 'center_y': .5}
            size_hint: (.5, None)
            height: '30sp'
            hint_text : 'Password'
        Label:
            id:
        Button:
            text: 'Submit'
            color: (1,0,0,1)
            on_press: app.user_login(root.username, root.password)

<Screen>:
    canvas.before:
        Color:
            rgba: (1, 1, 1, 1)
        Rectangle:
            pos: self.pos
            size: self.size
